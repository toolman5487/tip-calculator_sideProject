---
description: UI 元件設計遵循 HIG 守則，間距與尺寸以 4 或 8 的倍數設計
globs: "**/*.swift"
alwaysApply: true
---

# UI 元件設計規範

## Human Interface Guidelines (HIG)

- 元件設計與互動須遵循 [Apple Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/)
- 維持一致性：使用系統提供的控制項與樣式
- 確保觸控目標至少 44×44 pt
- 考量無障礙：支援 Dynamic Type、VoiceOver 等

## 4 / 8 倍數設計

間距、padding、尺寸等參數以 **4** 或 **8** 的倍數設計，以維持視覺節奏與對齊：

| 用途 | 建議值 | 範例 |
|------|--------|------|
| 最小間距 | 4, 8 | `inset(8)`, `spacing: 4` |
| 一般間距 | 8, 12, 16 | `offset(16)`, `top: 12` |
| 區塊間距 | 16, 24, 32 | `bottom: 24`, `inset(32)` |
| 大間距 | 24, 32, 40, 48 | section 間距、header 高度 |

```swift
view.snp.makeConstraints { make in
    make.leading.trailing.equalToSuperview().inset(16)
    make.top.equalToSuperview().offset(8)
}
section.contentInsets = NSDirectionalEdgeInsets(top: 0, leading: 16, bottom: 0, trailing: 16)
let cellSide: CGFloat = 96
```

- 圓角可為 4、8、12、16
- 字體大小依設計需求，不強制 4/8 倍數
